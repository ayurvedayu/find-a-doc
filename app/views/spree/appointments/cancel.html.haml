%h1 Please specify the reason of cancellation
= form_for [:account, @apt] do |f|
  = f.hidden_field :status, value: 'canceled'
  = f.hidden_field :canceled_by, value: @he
  %p
    = f.label :comment
    %br/
    = f.text_field :comment
  = f.submit 'Proceed'
