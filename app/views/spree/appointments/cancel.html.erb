<h1>Please specify the reason of cancellation</h1>
<%= form_for [:account, @spree_appointment] do |f| %>
	<%= f.hidden_field :status, value: 'canceled' %>
	<p>
		<%= f.label :comment %><br>
		<%= f.text_field :comment %>
	</p>
	<%= f.submit 'Proceed' %>
<% end %>