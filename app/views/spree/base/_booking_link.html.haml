%div
  - if empl.doctor.is_for_instant_booking?
    = link_to spree.new_account_appointment_path(empl), class: 'btn btn-success' do
      %span.glyphicon.glyphicon-flash
      Book an appointment
  - else
    = link_to empl.doctor.phone, "tel:#{empl.doctor.phone}"
